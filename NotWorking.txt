1) Panel split
2) Tabpanel active tab
3) Explicitly define a toolbar docket a bottom inside panel
4) Locked grids not working
5) TreePanelGrid Headers are not appearing
6) Dashboard, how can I define another ExtReact Component inside a Dashboard part
7) ExtGrid defined as React
8) Storybook does not work




9) you can no declare handlers of buttons inside toolbars -> CRITICAL
10) you can not render pure html inside a panel or a container
11) field triggers are not working

triggers = {{
				clean: {
					hidden: true,
					cls: 'x-fa fa-times',
					handler: function(comp) {
						comp.setValue(null);
						comp.getTrigger('clean').hide();
					}
				}
			}}